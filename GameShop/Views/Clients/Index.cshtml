@model IEnumerable<GameShop.Models.Client>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Client_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Client_Apellido)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TypeID.TypeID_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID_Number)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Client_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Client_Apellido)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TypeID.TypeID_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ID_Number)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Client_ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.Client_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Client_ID })
        </td>
    </tr>
}

</table>
